function localSortOne(items: string[]): string[] {
  const copy = [...items];
  copy.sort();
  return copy;
}

function localSortTwo(items: string[]): string[] {
  const copy = [...items];
  copy.sort();
  return copy;
}
